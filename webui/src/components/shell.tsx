import { TimeDisplay, usePreferences, type TimeMode } from "../preferences";
import { Activity, ChevronLeft, ChevronRight, Database, FlaskConical, Gauge, Hammer, KeyRound, Search, ShieldCheck } from "lucide-react";
import { type ReactNode, useEffect, useState } from "react";
import { createDataCenterClient } from "../lib/api";
import { PageHeader, StatusBadge } from "./ui";
import { freshnessOf } from "../hooks";
import type { ReadyState } from "../lib/api";

export type Tab = "overview" | "datasets" | "maintenance" | "runs" | "quality" | "explorer" | "operations";
const items: Array<{ key: Tab; label: string; icon: typeof Gauge }> = [
  { key: "overview", label: "Overview", icon: Gauge }, { key: "datasets", label: "Data catalog", icon: Database },
  { key: "maintenance", label: "Maintenance", icon: Hammer }, { key: "runs", label: "Runs", icon: Activity },
  { key: "quality", label: "Quality", icon: ShieldCheck },
  { key: "explorer", label: "Explorer", icon: Search }, { key: "operations", label: "Operations", icon: FlaskConical },
];

export function AppShell({ tab, onTab, health, apiKey, onApiKey, onRefresh, message, children }: { tab: Tab; onTab: (tab: Tab) => void; health: ReadyState | null; apiKey: string; onApiKey: (key: string) => void; onRefresh: () => void; message: string; children: ReactNode }) {
  const [accessOpen, setAccessOpen] = useState(false);
  const [username, setUsername] = useState("admin"); const [password, setPassword] = useState(""); const [currentPassword, setCurrentPassword] = useState(""); const [newPassword, setNewPassword] = useState(""); const [loggedIn, setLoggedIn] = useState(false); const [authInitialized, setAuthInitialized] = useState(true); const [authError, setAuthError] = useState(""); const [authMessage, setAuthMessage] = useState("");
  const { locale, setLocale, timeMode, setTimeMode, t } = usePreferences();
  const [clock, setClock] = useState(new Date());
  useEffect(() => { const id = window.setInterval(() => setClock(new Date()), 1000); return () => window.clearInterval(id); }, []);
  const toggleLocale = () => setLocale(locale === "zh-CN" ? "en-US" : "zh-CN");
  const client = createDataCenterClient(apiKey);
  useEffect(() => { void client.auth.status().then(result => setAuthInitialized(result.data.initialized)).catch(() => undefined); void client.auth.me().then(result => { setUsername(result.data.username); setLoggedIn(true); }).catch(() => setLoggedIn(false)); }, [apiKey]);
  const login = async () => { try { const result = await client.auth.login(username, password); setUsername(result.data.username); setLoggedIn(true); setPassword(""); setAuthError(""); } catch (error) { setAuthError(error instanceof Error ? error.message : "Login failed"); } };
  const initialize = async () => { try { await client.auth.initialize(username, password); setAuthInitialized(true); setPassword(""); setAuthMessage(t("Initialized. Sign in.")); setAuthError(""); } catch (error) { setAuthError(error instanceof Error ? error.message : "Initialization failed"); } };
  const changePassword = async () => { try { await client.auth.changePassword(currentPassword, newPassword); setLoggedIn(false); setCurrentPassword(""); setNewPassword(""); setAuthError(""); setAuthMessage(t("Password changed. Sign in again.")); } catch (error) { setAuthError(error instanceof Error ? error.message : "Password change failed"); } };
  const [collapsed, setCollapsed] = useState(false); const title = tab === "overview" ? "Good morning, data center" : items.find(item => item.key === tab)?.label ?? tab;
  const freshness = freshnessOf(health);
  return <div className={`app-shell${collapsed ? " shell-collapsed" : ""}`}><aside className="sidebar"><div className="brand"><span>MD</span><div><b>Market Data</b><small>Center / operations</small></div></div><button className="collapse-button" aria-label={collapsed ? "Expand sidebar" : "Collapse sidebar"} title={collapsed ? "Expand sidebar" : "Collapse sidebar"} onClick={() => setCollapsed(value => !value)}>{collapsed ? <ChevronRight size={15} /> : <ChevronLeft size={15} />}</button><nav>{items.map(({ key, label, icon: Icon }) => <button aria-label={key} title={label} className={tab === key ? "selected" : ""} onClick={() => onTab(key)} key={key}><Icon size={16} /><span>{t(label)}</span></button>)}</nav><div className="sidebar-foot"><StatusBadge tone={health?.status === "ready" ? "good" : "warn"}>API {health?.status ?? "unavailable"}</StatusBadge><StatusBadge tone={freshness === "fresh" ? "good" : freshness === "stale" ? "warn" : "neutral"}>Snapshot {freshness}</StatusBadge><small>{health?.software_version ?? "unknown"} · {health?.deployment_id ?? "development"}</small><button className="sidebar-refresh" onClick={onRefresh}>{t("Refresh data")}</button></div></aside><main className="main"><PageHeader eyebrow="Operations console" title={t(title)} actions={<><select aria-label="Time zone" value={timeMode} onChange={event => setTimeMode(event.target.value as TimeMode)}><option value="Asia/Shanghai">UTC+8</option><option value="UTC">UTC</option><option value="dual">UTC+8 / UTC</option></select><span className="current-time"><TimeDisplay value={clock.toISOString()} /></span><button className="locale-button" onClick={toggleLocale}>{locale === "zh-CN" ? "English" : "中文"}</button><StatusBadge tone={health?.capacity_status === "critical" ? "bad" : health?.capacity_status === "warning" ? "warn" : "good"}>{t("Capacity")} · {t(health?.capacity_status ?? "checking")}</StatusBadge><button className="access-button" onClick={() => setAccessOpen(value => !value)}><KeyRound size={15} /> {loggedIn ? username : t("Login")}</button></>} />{accessOpen && <div className="access-panel">{loggedIn ? <><span>{t("Signed in as")} <b>{username}</b></span><label>{t("Current password")}<input type="password" value={currentPassword} onChange={event => setCurrentPassword(event.target.value)} /></label><label>{t("New password")}<input type="password" minLength={12} value={newPassword} onChange={event => setNewPassword(event.target.value)} /></label><button disabled={!currentPassword || newPassword.length < 12} onClick={() => void changePassword()}>{t("Change password")}</button><button onClick={async () => { await client.auth.logout(); setLoggedIn(false); }}>{t("Logout")}</button></> : <><label>{t("Username")}<input autoComplete="username" value={username} onChange={event => setUsername(event.target.value)} /></label><label>{t("Password")}<input type="password" autoComplete="new-password" value={password} onChange={event => setPassword(event.target.value)} /></label>{authInitialized ? <button onClick={() => void login()}>{t("Login")}</button> : <><button onClick={() => void initialize()} disabled={password.length < 12}>{t("Initialize password")}</button><small>{t("Set the first password to enable login.")}</small></>}</>}{authError && <small className="protected-copy">{authError}</small>}{authMessage && <small>{authMessage}</small>}</div>}{message && <div className="notice" role="status">{message}</div>}{children}</main></div>;
}
