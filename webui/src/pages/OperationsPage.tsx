import { type FormEvent, useState } from "react";
import { ConfirmDialog, EmptyState, MetricCard, PanelHeading, StatusBadge } from "../components/ui";
import { createDataCenterClient, type Metrics, type ReadyState } from "../lib/api";

const bytes = (value?: number) => value == null ? "—" : `${(value / 1024 ** 3).toFixed(1)} GiB`;
export function OperationsPage({ apiKey, health, metrics, onChanged, onMessage }: { apiKey: string; health: ReadyState | null; metrics: Metrics | null; onChanged: () => void; onMessage: (message: string) => void }) {
  const [provider, setProvider] = useState("fixture"); const [symbol, setSymbol] = useState("UI_TEST"); const [start, setStart] = useState("2026-01-01"); const [end, setEnd] = useState("2026-01-03"); const [confirm, setConfirm] = useState(false); const [busy, setBusy] = useState(false);
  const submit = (event: FormEvent) => { event.preventDefault(); setConfirm(true); }; const ingest = async () => { setBusy(true); try { const result = await createDataCenterClient(apiKey).ingest({ job_id: `webui-${Date.now()}`, run_scope: "acceptance", provider, symbol, asset_class: "test", timeframe: "1d", start: `${start}T00:00:00Z`, end: `${end}T00:00:00Z` }); onMessage(`Queued run ${result.data.run_id}`); setConfirm(false); onChanged(); } catch (reason) { onMessage(reason instanceof Error ? reason.message : "Unable to queue ingest"); } finally { setBusy(false); } };
  const capacity = metrics?.capacity;
  const alerts = [
    capacity && capacity.status !== "ok" ? { title: `Capacity ${capacity.status}`, detail: `${(capacity.free_ratio * 100).toFixed(1)}% free`, tone: capacity.status === "critical" ? "bad" as const : "warn" as const } : null,
    (metrics?.dead_letter_by_state?.active ?? 0) > 0 ? { title: "Active dead letters", detail: `${metrics?.dead_letter_by_state?.active} require review`, tone: "bad" as const } : null,
    metrics?.operational_snapshot_status && metrics.operational_snapshot_status !== "fresh" ? { title: "Operational snapshot", detail: metrics.operational_snapshot_status, tone: "warn" as const } : null,
  ].filter(Boolean) as Array<{ title: string; detail: string; tone: "warn" | "bad" }>;
  return <div className="operations-page"><section className="metrics operations-metrics"><MetricCard label="Capacity free" value={capacity ? `${(capacity.free_ratio * 100).toFixed(1)}%` : "—"} note={`${bytes(capacity?.free_bytes)} free`} tone={capacity?.status === "critical" ? "bad" : capacity?.status === "warning" ? "warn" : undefined} /><MetricCard label="Queue depth" value={String(metrics?.queue_depth ?? 0)} note={`oldest ${Math.round(metrics?.queue_oldest_age_seconds ?? 0)}s`} /><MetricCard label="Dead letters" value={String(metrics?.dead_letter_total ?? 0)} note={`${metrics?.retry_attempts_total ?? 0} retry attempts`} /><MetricCard label="Temporary backups" value={String(metrics?.temporary_backup_count ?? 0)} note={`snapshot ${metrics?.operational_snapshot_status ?? "unknown"}`} /></section><div className="operations-grid"><section className="panel"><PanelHeading eyebrow="Storage protection" title="Capacity and recovery" action={<StatusBadge tone={capacity?.status === "critical" ? "bad" : capacity?.status === "warning" ? "warn" : "good"}>{capacity?.status ?? "unknown"}</StatusBadge>} />{capacity ? <><div className="capacity-meter"><i style={{ width: `${Math.min(100, (1 - capacity.free_ratio) * 100)}%` }} /></div><dl className="detail-list"><div><dt>Total</dt><dd>{bytes(capacity.total_bytes)}</dd></div><div><dt>Used</dt><dd>{bytes(capacity.used_bytes)}</dd></div><div><dt>Warning threshold</dt><dd>{(capacity.warning_free_ratio * 100).toFixed(0)}%</dd></div><div><dt>Critical threshold</dt><dd>{(capacity.critical_free_ratio * 100).toFixed(0)}%</dd></div><div><dt>Latest backup</dt><dd>{metrics?.last_successful_backup_at ?? "Not recorded"}</dd></div><div><dt>Latest recovery drill</dt><dd>{metrics?.last_successful_recovery_drill_at ?? "Not recorded"}</dd></div></dl></> : <EmptyState title="Capacity unavailable" />}</section><section className="panel"><PanelHeading eyebrow="Deployment identity" title="Runtime" /><dl className="detail-list"><div><dt>Deployment</dt><dd className="mono">{health?.deployment_id ?? "—"}</dd></div><div><dt>Version</dt><dd>{health?.software_version ?? "—"}</dd></div><div><dt>Source commit</dt><dd className="mono">{health?.source_commit ?? "—"}</dd></div><div><dt>Read path</dt><dd>{health?.read_status ?? "—"}</dd></div><div><dt>Write path</dt><dd>{health?.write_status ?? "—"}</dd></div><div><dt>Worker heartbeat</dt><dd>{health?.worker_heartbeat_age_seconds == null ? "—" : `${Math.round(health.worker_heartbeat_age_seconds)}s`}</dd></div></dl></section><section className="panel alerts-panel"><PanelHeading eyebrow="Aggregated signals" title="Active alerts" />{alerts.length ? <div className="alert-list">{alerts.map(alert => <div key={alert.title}><StatusBadge tone={alert.tone}>{alert.title}</StatusBadge><span>{alert.detail}</span></div>)}</div> : <EmptyState title="No active alerts" detail="Capacity, dead letters and operational snapshot are clear." />}</section><section className="panel operation-command"><PanelHeading eyebrow="Authorized command" title="Queue ingest" /><form onSubmit={submit}><label>Provider<input value={provider} onChange={event => setProvider(event.target.value)} /></label><label>Symbol<input value={symbol} onChange={event => setSymbol(event.target.value)} /></label><label>Start<input type="date" value={start} onChange={event => setStart(event.target.value)} /></label><label>End<input type="date" value={end} onChange={event => setEnd(event.target.value)} /></label><button className="primary-button" type="submit" disabled={health?.write_status === "protected"}>Review ingest</button></form>{health?.write_status === "protected" && <p className="protected-copy">Capacity protection is active. Reads and recovery remain available.</p>}</section></div>{confirm && <ConfirmDialog title="Queue ingest run?" detail={`${provider} ${symbol}, ${start} through ${end}. This creates an asynchronous acceptance run.`} confirmLabel="Queue ingest" busy={busy} onConfirm={() => void ingest()} onCancel={() => setConfirm(false)} />}</div>;
}
