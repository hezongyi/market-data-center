# MVP Plan

1. 建立 Python backend package、配置加载和领域模型。
2. 实现 fixture connector 与 `provider_bars` ingest 闭环。
3. 实现 FastAPI health、datasets、runs、bars、quality 接口。
4. 建立 React/Vite Web UI，完成 Overview、Runs、Data Explorer。
5. 增加 API/worker/systemd 本地启动方式和验收脚本。
6. 在 `macro-market-lab` 中用 client 完成只读查询对接。

