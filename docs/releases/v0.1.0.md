# v0.1.0 — production readiness baseline

Released from protected `main` commit `679dafff539d8e64938cd098f61088e173ae114d` after required GitHub Actions run `34442430045` succeeded on 2026-09-10.

- API: `/api/v1` envelope and governed provider/economic read and ingest endpoints.
- Dataset schemas: `provider_bars.v1`, `economic_observations.v1` read compatibility, `economic_observations.v2` write default.
- Manifest: immutable published manifests for supported dataset schemas.
- Receipts: immutable terminal run receipts; retry creates a new run.
- Runtime: Python 3.10+ declaration; Node 22 Web UI build baseline.
- Providers: Binance, yfinance, and FRED; fixture is used for deterministic acceptance.
- Operations: worker supervision, metrics, alert outbox, staging cleanup, backup v1 restore, systemd units/timers, and consumer parity evidence.

Rollback: check out the immutable previous tag/commit, restore its environment, restart API/worker, and run `scripts/smoke.sh`; canonical history and terminal receipts must not be rewritten.
